; Excel批量生成工具配置文件
; 路径支持相对路径（相对于程序所在目录）或绝对路径

[PATHS]
; 配置文件存放目录（默认 config）
config_dir = config
; 日志文件存放目录（默认 logs）
log_dir = logs

[LOG]
; 日志级别: DEBUG, INFO, WARNING, ERROR
level = INFO
; 保留最近日志文件数
max_files = 30
; 是否输出到控制台
console_output = true
; 是否写入日志文件
file_output = true